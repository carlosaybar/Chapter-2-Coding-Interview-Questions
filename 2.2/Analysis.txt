This algorithm has a time complexity of O(n), n being the size/length of the list. 
The algorithm has to iterate through the entire list in order to get the node count,
so the longer the list, the more time it will take to execute the algorithm.
